apiVersion: v1
kind: Namespace
metadata:
  name:  ns-posthub--env--
  labels:
    name: ns-posthub--env--


---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-mongo
  namespace: ns-posthub--env--
spec:
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  selector:
    matchLabels:
      app: users-mongo
  template:
    metadata:
      labels:
        app: users-mongo
    spec:
      containers:
      - name: users-mongo
        image: mongo
        resources:
          requests:
              cpu: "250m"
              memory: "250Mi"
          limits:
            memory: "1500Mi"
            cpu: "2500m"
        ports:
        - containerPort: 27017
          name: users-mongo


---
kind: Service
apiVersion: v1
metadata:
  name: users-mongo-service
  namespace: ns-posthub--env--
spec:
  selector:
    name: users-mongo
  type: ClusterIP
  ports:
  - name: users-mongo
    port: 27020
    targetPort: 27017